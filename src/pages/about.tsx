import type { NextPage } from 'next'
import { withMenu } from '../shared/layout/withHeader'

const AboutProjectPage: NextPage = () => {
   return (
      <div>About Project</div>
   )
}

export default withMenu(AboutProjectPage)
